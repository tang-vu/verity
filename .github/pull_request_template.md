## Summary

<!-- What does this PR change and why? -->

## Component

<!-- contracts / oracle-agent / defi-agent / shared / web / scripts / docs -->

## Checklist

- [ ] `npm run typecheck` passes
- [ ] `npm run test:agent` passes
- [ ] `cargo test` in `contracts/` passes (if contracts changed)
- [ ] No keys, `.env` values, or other secrets committed
- [ ] Conventional commit message (`feat:`, `fix:`, `docs:`, …)
