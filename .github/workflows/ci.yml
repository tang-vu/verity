name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Least privilege: CI only reads the repo, never writes.
permissions:
  contents: read

jobs:
  typescript:
    name: TypeScript (typecheck + agent tests)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Typecheck all workspaces
        run: npm run typecheck

      - name: Run agent + shared unit tests
        run: npm test

  contracts:
    name: Contracts (Odra / cargo test)
    runs-on: ubuntu-latest
    env:
      # rust-toolchain.toml pins the Windows-GNU host triple for local dev;
      # override with a plain nightly so the Linux runner resolves its own host.
      RUSTUP_TOOLCHAIN: nightly
    defaults:
      run:
        working-directory: contracts
    steps:
      - uses: actions/checkout@v7

      - name: Install nightly toolchain
        run: rustup toolchain install nightly --profile minimal

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: contracts

      - name: Run contract test suite (OdraVM)
        run: cargo test --locked
