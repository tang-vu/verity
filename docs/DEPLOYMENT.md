# DEPLOYMENT — Casper testnet (`casper-test`)

Authoritative record of on-chain artifacts. Hashes/links are filled at deploy time.
Explorer base: https://testnet.cspr.live

## Accounts (generated locally; fund at the faucet)

| Role | Public key | Account hash | Funded? |
|---|---|---|---|
| Producer (oracle) | `016a6a79b53698c0a5205988e4a2d42dc0aea30735f5d41b55e65a2764e72c0cdc` | `971c1bd6ad47eff8cd815d53082a66e1246bf0ce09969c3dfca771c6a71d247d` | ⏳ fund me |
| Consumer (DeFi agent) | `01f06b02c33c1408b7f61758ba39a8f513bf556a390db5b0a71ceabf457520c343` | `82ec56fef048bec1bec5811f123c1460615ba1514b6b1b886ce3c5b97a15d780` | ⏳ fund me |

- Producer account: https://testnet.cspr.live/account/016a6a79b53698c0a5205988e4a2d42dc0aea30735f5d41b55e65a2764e72c0cdc
- Consumer account: https://testnet.cspr.live/account/01f06b02c33c1408b7f61758ba39a8f513bf556a390db5b0a71ceabf457520c343
- **Faucet:** https://testnet.cspr.live/tools/faucet (request CSPR for *both* public keys)

> These keypairs were generated by `npm run keygen` for the buildathon. The private
> PEMs live in `./keys/` and are gitignored. Regenerate anytime with `npm run keygen --force`.

## Contract: SignalOracle

| Field | Value |
|---|---|
| Package hash | `⏳ <SIGNAL_ORACLE_PACKAGE_HASH after deploy>` |
| Contract hash (latest version) | `⏳ <filled after deploy>` |
| Install/deploy tx | `⏳ <SIGNAL_ORACLE_DEPLOY_HASH>` |
| WASM size | 310 KB (`contracts/wasm/SignalOracle.wasm`) |
| Contract package link | `https://testnet.cspr.live/contract-package/<package-hash>` |
| Deploy tx link | `https://testnet.cspr.live/deploy/<deploy-hash>` |

Deploy with: `npm run deploy:sdk` (Windows-friendly, casper-js-sdk) — writes the
package + deploy hashes back into `.env` and prints the links to paste here.
On Linux/macOS you may instead use the Odra livenet deployer:
`cd contracts && cargo run --bin deploy_signal_oracle --features livenet`.

## Seeded reputation history (real publish+resolve txs)

Run `npm run seed` (4 signals: 3 correct, 1 wrong → 75% accuracy). Record below:

| Signal # | Direction | Outcome | Publish tx | Resolve tx |
|---|---|---|---|---|
| 0 | UP | CORRECT | `⏳` | `⏳` |
| 1 | DOWN | CORRECT | `⏳` | `⏳` |
| 2 | UP | CORRECT | `⏳` | `⏳` |
| 3 | UP | WRONG | `⏳` | `⏳` |

## Live signal (LLM-generated, real data)

Run `npm run oracle:publish`. Record:

| Field | Value |
|---|---|
| Signal id | `⏳` |
| Publish tx | `⏳` |
| Publish tx link | `⏳` |

## x402 paid read + reputation-weighted action (the loop)

Run `npm run agent:loop` (oracle server up). Record:

| Step | Value |
|---|---|
| x402 settlement tx | `⏳` |
| Settlement tx link | `⏳` |
| Reputation-weighted swap tx (CSPR.trade) | `⏳` |
| Swap tx link | `⏳` |

## x402 payment asset (CEP-18)

| Field | Value |
|---|---|
| Asset package hash (`X402_ASSET_PACKAGE_HASH`) | `⏳ <filled by deploy:x402-token>` |
| Deploy tx | `⏳` |
| Name / symbol / decimals | `x402USD` / `x402` / `2` |
| Contract | `contracts/src/x402_token.rs` (X402Token = CEP-18 + CEP-3009 + CEP-2612) |
| Consumer funding tx | `⏳` |
| Facilitator | `https://x402-facilitator.cspr.cloud` |
| CAIP-2 network | `casper:casper-test` |

Deploy our own token + fund the consumer in one step: `npm run deploy:x402-token`
(writes `X402_ASSET_PACKAGE_HASH` to `.env`). Until set, the paywall runs in
**verified-deferred** mode (signature verified locally), flipping to full
facilitator settlement automatically once the token hash + `CSPR_CLOUD_ACCESS_TOKEN`
are present. If the buildathon publishes a canonical x402 demo token, point
`X402_ASSET_PACKAGE_HASH` at it instead.
